{
  "name": "reliquia-interiores",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page da Relíquia Interiores, reproduzida a partir do layout oficial.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "check": "node --check script.js && node --check site.config.js && node --check scripts/serve.mjs && node --check scripts/build.mjs"
  },
  "engines": { "node": ">=20" }
}
